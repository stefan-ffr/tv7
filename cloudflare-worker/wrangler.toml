name = "xtream-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Cloudflare Account Settings
# account_id = "your-account-id"  # Optional: Ihre Cloudflare Account ID

# Workers Routes (optional, für Custom Domain)
# routes = [
#   { pattern = "xtream.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

# Environment Variables (optional)
[vars]
ENVIRONMENT = "production"
